@using EDiary.Models
@using EDiary.ViewModels
@model IEnumerable<TeacherGroupSubjectModel>
<table class="table table-hover table-bordered">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Предмет</th>
            <th scope="col">Преподаватель</th>
            <th scope="col">Группа</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var teacherGroupSubject in Model)
        {
            <tr>
                <td>@teacherGroupSubject.tsubjectId</td>
                <td>@teacherGroupSubject.subjectName</td>
                <td>@teacherGroupSubject.teacherSurname @teacherGroupSubject.teacherName @teacherGroupSubject.teacherLastname</td>
                <td>@teacherGroupSubject.groups</td>
            </tr>
        }
    </tbody>
</table>